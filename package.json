{
  "name": "wtr-bagtracing-bot",
  "version": "1.0.0",
  "description": "This bot will trace the bag after getting input for bag tag number or pnr number",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "bot",
    "bagTracing",
    "botbuilder"
  ],
  "author": "code sniffers",
  "license": "ISC",
  "dependencies": {
    "botbuilder": "^3.11.0",
    "restify": "^6.3.1"
  }
}
